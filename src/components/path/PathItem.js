import React from 'react'

const PathItem = ({title}) => {
    return (
        <div className="K1">
        <h1 style={{
            color: "black"
        }}>{title}</h1>
    </div>
    )
}

export default PathItem
